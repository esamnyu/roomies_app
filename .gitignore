# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
# ----------------------------------------------------------------------
/node_modules
/.pnp
.pnp.*
# Lockfiles (package-lock.json is usually committed, yarn.lock always is)
# Keep package-lock.json if using npm and it's in the repo.
# If using Yarn 1.x, yarn.lock is committed.
# For Yarn Berry (2+), .yarn/* is used, except for specified folders.
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
!.yarn/sdks # Often useful to commit for editor integration

# Testing
# ----------------------------------------------------------------------
/coverage

# Next.js specific
# ----------------------------------------------------------------------
/.next/
/out/
next-env.d.ts # Automatically generated by Next.js

# Production & Build outputs (general)
# ----------------------------------------------------------------------
/build # General build output, can be project specific

# TypeScript
# ----------------------------------------------------------------------
*.tsbuildinfo

# Sensitive Environment Variables & Configuration
# NEVER commit files ending in .local or containing sensitive secrets.
# It is recommended to use a .env.example file for non-sensitive default values.
# ----------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
# If you have other .env files that are NOT .local but ARE sensitive, list them.
# e.g., credentials.env

# Vercel
# ----------------------------------------------------------------------
.vercel

# Miscellaneous
# ----------------------------------------------------------------------
.DS_Store # macOS
*.pem # Private key files
*.log # General log files (can be too broad, be specific if possible)

# Debug logs
# ----------------------------------------------------------------------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editor & IDE specific files
# ----------------------------------------------------------------------
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*~ # Backup files created by some editors
*.bak
*.orig

# OS Specific
# ----------------------------------------------------------------------
Thumbs.db # Windows
desktop.ini # Windows
